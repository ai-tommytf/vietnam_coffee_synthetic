[project]
name = "vietnam-coffee-synthetic"
version = "0.1.0"
description = "Generate synthetic Vietnam coffee yield data for conference booth"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.8.0",
    "numpy>=1.26.0",
    "pandas>=2.1.0",
]
